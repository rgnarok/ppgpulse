{
  "name": "ppg-pulse",
  "version": "1.0.0",
  "private": true,
  "description": "PPG Pulse — delivery & performance cockpit for VAYUZ People Group",
  "engines": {
    "node": ">=20 <23"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "test": "npm run test -w server && npm run test -w web",
    "lint": "eslint .",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "verify": "npm run typecheck && npm run lint && npm run test && npm run build",
    "seed": "npm run seed -w server",
    "reset": "npm run reset -w server",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
